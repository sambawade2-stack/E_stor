; Limites d'upload pour l'admin (photos produits/catégories/marques).
; Pris en compte automatiquement par PHP-FPM en production (Apache/Nginx).
; Sans effet sur `php artisan serve` : utilisez `composer run serve` en local.
upload_max_filesize = 10M
post_max_size = 40M
max_file_uploads = 20
